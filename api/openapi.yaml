openapi: 3.0.3
info:
  title: HeartFolio API
  description: |
    HeartFolio transforms emotional pain and healing processes into verifiable soft-skill assets.

    ## JSON:API

    All endpoints conform to the [JSON:API v1.0](https://jsonapi.org/) specification.

    **Content-Type:** `application/vnd.api+json`

    ### Response envelope

    Every response is wrapped in a top-level `data` object (single resource) or `data` array (collection).

    ```json
    { "data": { "type": "users", "id": "abc123", "attributes": { ... } } }
    ```

    ### Error envelope

    All errors return an `errors` array instead of `data`.

    ```json
    { "errors": [{ "status": "404", "title": "Not Found", "detail": "badge not found" }] }
    ```

    ### Relationships

    Resources that embed related data include a `relationships` key whose value is a full
    resource object (not just a linkage), making it a single-request pattern.

    ### Pagination

    Collection endpoints support pagination via query parameters:
    - `page[offset]` — number of items to skip (default: 0)
    - `page[limit]` — max items to return (default: 20, max: 100)

    Paginated responses include a `meta` object: `{ "total", "offset", "limit" }`.

    ### Authentication

    Protected endpoints require `Authorization: Bearer <token>`.
    Obtain a token from `POST /api/v1/users/login` or `POST /api/v1/users/register`.
  version: 1.0.0

servers:
  - url: http://localhost:8080
    description: Local development

tags:
  - name: Health
    description: Health check
  - name: Users
    description: Registration, login, profile, privacy, mood tracking
  - name: Portfolio
    description: Emotional portfolio — aggregated profile with skills, badges, and mood
  - name: Badges
    description: Badge library and user badge management
  - name: Quests
    description: Quest library, adaptive quests, quest lifecycle
  - name: "Admin: Badges"
    description: Admin badge management (requires admin role)
  - name: "Admin: Quests"
    description: Admin quest management (requires admin role)

components:
  securitySchemes:
    BearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

  # ── Reusable schemas ──────────────────────────────────────────────────────

  schemas:

    # ── JSON:API primitives ──────────────────────────────────────────

    ErrorObject:
      type: object
      properties:
        status:
          type: string
          example: "404"
        title:
          type: string
          example: "Not Found"
        detail:
          type: string
          example: "badge not found"

    ErrorDocument:
      type: object
      required: [errors]
      properties:
        errors:
          type: array
          items:
            $ref: "#/components/schemas/ErrorObject"

    # ── Attribute bags ────────────────────────────────────────────────

    PrivacySettings:
      type: object
      properties:
        show_badges:
          type: boolean
          example: true
        show_stats:
          type: boolean
          example: true
        show_quests:
          type: boolean
          example: true

    UserAttributes:
      type: object
      properties:
        email:
          type: string
          format: email
          example: "user@example.com"
        role:
          type: string
          enum: [user, admin]
          example: "user"
        exp:
          type: integer
          format: int64
          example: 250
        level:
          type: integer
          format: int32
          example: 3
        tier:
          type: string
          enum: [bronze, silver, gold, platinum, diamond]
          example: "silver"
        privacy:
          $ref: "#/components/schemas/PrivacySettings"

    AuthAttributes:
      allOf:
        - $ref: "#/components/schemas/UserAttributes"
        - type: object
          properties:
            token:
              type: string
              example: "eyJhbGciOiJIUzI1NiIs..."

    BadgeAttributes:
      type: object
      properties:
        name:
          type: string
          example: "Burnout Survivor"
        description:
          type: string
          example: "Awarded for surviving a burnout period"
        category:
          type: string
          example: "resilience"
        max_level:
          type: integer
          format: int32
          example: 5
        criteria:
          type: string
          example: "Complete 3 self-care quests"
        icon:
          type: string
          example: "https://example.com/icons/burnout.png"
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time

    UserBadgeAttributes:
      type: object
      properties:
        level:
          type: integer
          format: int32
          example: 2
        earned_at:
          type: string
          format: date-time
        last_updated:
          type: string
          format: date-time

    SkillBadgeInfo:
      type: object
      properties:
        badge_id:
          type: string
        name:
          type: string
          example: "Burnout Survivor"
        level:
          type: integer
          format: int32
          example: 2
        max_level:
          type: integer
          format: int32
          example: 5

    SkillStatsAttributes:
      type: object
      properties:
        category:
          type: string
          example: "resilience"
        total_level:
          type: integer
          format: int32
          example: 8
        badge_count:
          type: integer
          format: int32
          example: 3
        badges:
          type: array
          items:
            $ref: "#/components/schemas/SkillBadgeInfo"

    MoodEntryAttributes:
      type: object
      properties:
        mood:
          type: string
          example: "anxious"
        note:
          type: string
          example: "Feeling overwhelmed today"
        created_at:
          type: string
          format: date-time

    QuestSummary:
      type: object
      properties:
        total_started:
          type: integer
          format: int32
          example: 10
        total_completed:
          type: integer
          format: int32
          example: 7
        total_abandoned:
          type: integer
          format: int32
          example: 1
        total_exp_earned:
          type: integer
          format: int64
          example: 350

    PortfolioAttributes:
      type: object
      properties:
        email:
          type: string
          format: email
        level:
          type: integer
          format: int32
        exp:
          type: integer
          format: int64
        tier:
          type: string
          enum: [bronze, silver, gold, platinum, diamond]
        total_badges:
          type: integer
          format: int32
        quest_summary:
          $ref: "#/components/schemas/QuestSummary"

    QuestAttributes:
      type: object
      properties:
        name:
          type: string
          example: "Solitude Strength"
        description:
          type: string
          example: "Practice being comfortable alone for 30 minutes"
        category:
          type: string
          example: "self-care"
        exp_reward:
          type: integer
          format: int64
          example: 50
        mood_tags:
          type: array
          items:
            type: string
          example: ["lonely", "anxious"]
        difficulty:
          type: string
          enum: [easy, medium, hard]
          example: "easy"
        estimated_duration:
          type: integer
          format: int32
          description: Duration in minutes
          example: 30
        badge_level:
          type: integer
          format: int32
          description: Badge level awarded on completion (0 = no badge)
          example: 1
        is_active:
          type: boolean
          example: true
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time

    UserQuestAttributes:
      type: object
      properties:
        status:
          type: string
          enum: [active, completed, abandoned]
          example: "active"
        started_at:
          type: string
          format: date-time
        completed_at:
          type: string
          format: date-time
          nullable: true
        exp_earned:
          type: integer
          format: int64
          example: 50

    # ── Resource objects ───────────────────────────────────────────────

    UserResource:
      type: object
      properties:
        type:
          type: string
          enum: [users]
        id:
          type: string
          example: "665f1a2b3c4d5e6f7a8b9c0d"
        attributes:
          $ref: "#/components/schemas/UserAttributes"

    AuthResource:
      type: object
      properties:
        type:
          type: string
          enum: [users]
        id:
          type: string
          example: "665f1a2b3c4d5e6f7a8b9c0d"
        attributes:
          $ref: "#/components/schemas/AuthAttributes"

    BadgeResource:
      type: object
      properties:
        type:
          type: string
          enum: [badges]
        id:
          type: string
          example: "665f1a2b3c4d5e6f7a8b9c0d"
        attributes:
          $ref: "#/components/schemas/BadgeAttributes"

    UserBadgeResource:
      type: object
      properties:
        type:
          type: string
          enum: [user-badges]
        id:
          type: string
          example: "665f1a2b3c4d5e6f7a8b9c0d"
        attributes:
          $ref: "#/components/schemas/UserBadgeAttributes"
        relationships:
          type: object
          properties:
            badge:
              type: object
              properties:
                data:
                  $ref: "#/components/schemas/BadgeResource"

    SkillStatsResource:
      type: object
      properties:
        type:
          type: string
          enum: [skill-stats]
        id:
          type: string
          description: The category name
          example: "resilience"
        attributes:
          $ref: "#/components/schemas/SkillStatsAttributes"

    MoodEntryResource:
      type: object
      properties:
        type:
          type: string
          enum: [mood-entries]
        id:
          type: string
        attributes:
          $ref: "#/components/schemas/MoodEntryAttributes"

    PortfolioResource:
      type: object
      properties:
        type:
          type: string
          enum: [portfolios]
        id:
          type: string
          description: The user ID
        attributes:
          $ref: "#/components/schemas/PortfolioAttributes"
        relationships:
          type: object
          properties:
            skill_stats:
              type: object
              properties:
                data:
                  type: array
                  items:
                    $ref: "#/components/schemas/SkillStatsResource"
            recent_moods:
              type: object
              properties:
                data:
                  type: array
                  items:
                    $ref: "#/components/schemas/MoodEntryResource"

    PrivacyResource:
      type: object
      properties:
        type:
          type: string
          enum: [privacy-settings]
        id:
          type: string
        attributes:
          $ref: "#/components/schemas/PrivacySettings"

    QuestResource:
      type: object
      properties:
        type:
          type: string
          enum: [quests]
        id:
          type: string
          example: "665f1a2b3c4d5e6f7a8b9c0d"
        attributes:
          $ref: "#/components/schemas/QuestAttributes"

    UserQuestResource:
      type: object
      properties:
        type:
          type: string
          enum: [user-quests]
        id:
          type: string
          example: "665f1a2b3c4d5e6f7a8b9c0d"
        attributes:
          $ref: "#/components/schemas/UserQuestAttributes"

    UserQuestDetailResource:
      type: object
      properties:
        type:
          type: string
          enum: [user-quests]
        id:
          type: string
        attributes:
          $ref: "#/components/schemas/UserQuestAttributes"
        relationships:
          type: object
          properties:
            quest:
              type: object
              properties:
                data:
                  $ref: "#/components/schemas/QuestResource"

    MessageResource:
      type: object
      properties:
        type:
          type: string
          example: "badges"
        id:
          type: string
        attributes:
          type: object
          properties:
            message:
              type: string
              example: "operation successful"

    # ── Top-level documents ────────────────────────────────────────────

    UserDocument:
      type: object
      required: [data]
      properties:
        data:
          $ref: "#/components/schemas/UserResource"

    AuthDocument:
      type: object
      required: [data]
      properties:
        data:
          $ref: "#/components/schemas/AuthResource"

    BadgeDocument:
      type: object
      required: [data]
      properties:
        data:
          $ref: "#/components/schemas/BadgeResource"

    BadgeCollectionDocument:
      type: object
      required: [data]
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/BadgeResource"

    UserBadgeCollectionDocument:
      type: object
      required: [data]
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/UserBadgeResource"

    SkillStatsCollectionDocument:
      type: object
      required: [data]
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/SkillStatsResource"

    MoodEntryDocument:
      type: object
      required: [data]
      properties:
        data:
          $ref: "#/components/schemas/MoodEntryResource"

    MoodEntryCollectionDocument:
      type: object
      required: [data]
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/MoodEntryResource"

    PortfolioDocument:
      type: object
      required: [data]
      properties:
        data:
          $ref: "#/components/schemas/PortfolioResource"

    PrivacyDocument:
      type: object
      required: [data]
      properties:
        data:
          $ref: "#/components/schemas/PrivacyResource"

    QuestDocument:
      type: object
      required: [data]
      properties:
        data:
          $ref: "#/components/schemas/QuestResource"

    QuestCollectionDocument:
      type: object
      required: [data]
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/QuestResource"

    UserQuestDocument:
      type: object
      required: [data]
      properties:
        data:
          $ref: "#/components/schemas/UserQuestResource"

    UserQuestDetailCollectionDocument:
      type: object
      required: [data]
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/UserQuestDetailResource"

    MessageDocument:
      type: object
      required: [data]
      properties:
        data:
          $ref: "#/components/schemas/MessageResource"

  # ── Reusable parameters ────────────────────────────────────────────────────

  parameters:
    PageOffset:
      name: "page[offset]"
      in: query
      required: false
      schema:
        type: integer
        minimum: 0
        default: 0
      description: Number of items to skip
    PageLimit:
      name: "page[limit]"
      in: query
      required: false
      schema:
        type: integer
        minimum: 1
        maximum: 100
        default: 20
      description: Maximum items to return (max 100)

  # ── Reusable responses ─────────────────────────────────────────────────────

  responses:
    Unauthorized:
      description: Missing or invalid token
      content:
        application/vnd.api+json:
          schema:
            $ref: "#/components/schemas/ErrorDocument"
          example:
            errors:
              - status: "401"
                title: "Unauthorized"
                detail: "missing authorization header"

    Forbidden:
      description: Insufficient permissions
      content:
        application/vnd.api+json:
          schema:
            $ref: "#/components/schemas/ErrorDocument"
          example:
            errors:
              - status: "403"
                title: "Forbidden"
                detail: "admin access required"

    NotFound:
      description: Resource not found
      content:
        application/vnd.api+json:
          schema:
            $ref: "#/components/schemas/ErrorDocument"
          example:
            errors:
              - status: "404"
                title: "Not Found"
                detail: "resource not found"

    BadRequest:
      description: Invalid request body or parameters
      content:
        application/vnd.api+json:
          schema:
            $ref: "#/components/schemas/ErrorDocument"

# ── Paths ──────────────────────────────────────────────────────────────────────

paths:

  # ── Health ─────────────────────────────────────────────────────────────────

  /ping:
    get:
      tags: [Health]
      summary: Health check
      responses:
        "200":
          description: Service is running
          content:
            text/plain:
              schema:
                type: string
                example: pong

  # ── Users ──────────────────────────────────────────────────────────────────

  /api/v1/users/register:
    post:
      tags: [Users]
      summary: Register a new user
      description: Creates a new account and returns a JWT token.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [email, password]
              properties:
                email:
                  type: string
                  format: email
                  example: "user@example.com"
                password:
                  type: string
                  format: password
                  example: "secret123"
      responses:
        "201":
          description: User created
          content:
            application/vnd.api+json:
              schema:
                $ref: "#/components/schemas/AuthDocument"
              example:
                data:
                  type: users
                  id: "665f1a2b3c4d5e6f7a8b9c0d"
                  attributes:
                    email: "user@example.com"
                    role: "user"
                    token: "eyJhbGciOiJIUzI1NiIs..."
        "400":
          $ref: "#/components/responses/BadRequest"
        "409":
          description: User already exists
          content:
            application/vnd.api+json:
              schema:
                $ref: "#/components/schemas/ErrorDocument"

  /api/v1/users/login:
    post:
      tags: [Users]
      summary: Login
      description: Validates credentials and returns a JWT token.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [email, password]
              properties:
                email:
                  type: string
                  format: email
                password:
                  type: string
                  format: password
      responses:
        "200":
          description: Login successful
          content:
            application/vnd.api+json:
              schema:
                $ref: "#/components/schemas/AuthDocument"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/users/me:
    get:
      tags: [Users]
      summary: Get current user profile
      description: Returns profile including tier ranking and privacy settings.
      security:
        - BearerAuth: []
      responses:
        "200":
          description: Current user with tier and privacy
          content:
            application/vnd.api+json:
              schema:
                $ref: "#/components/schemas/UserDocument"
              example:
                data:
                  type: users
                  id: "665f1a2b3c4d5e6f7a8b9c0d"
                  attributes:
                    email: "user@example.com"
                    role: "user"
                    exp: 250
                    level: 3
                    tier: "silver"
                    privacy:
                      show_badges: true
                      show_stats: true
                      show_quests: true
        "401":
          $ref: "#/components/responses/Unauthorized"
    patch:
      tags: [Users]
      summary: Update profile
      description: Update the authenticated user's email.
      security:
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                email:
                  type: string
                  format: email
                  example: "newemail@example.com"
      responses:
        "200":
          description: Profile updated
          content:
            application/vnd.api+json:
              schema:
                $ref: "#/components/schemas/UserDocument"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          description: Email already in use
          content:
            application/vnd.api+json:
              schema:
                $ref: "#/components/schemas/ErrorDocument"

  /api/v1/users/me/password:
    post:
      tags: [Users]
      summary: Change password
      description: Verifies old password and sets a new one. Minimum 8 characters.
      security:
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [old_password, new_password]
              properties:
                old_password:
                  type: string
                  format: password
                new_password:
                  type: string
                  format: password
                  minLength: 8
      responses:
        "200":
          description: Password changed
          content:
            application/vnd.api+json:
              schema:
                $ref: "#/components/schemas/MessageDocument"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          description: Incorrect old password
          content:
            application/vnd.api+json:
              schema:
                $ref: "#/components/schemas/ErrorDocument"

  /api/v1/users/me/privacy:
    patch:
      tags: [Users]
      summary: Update privacy settings
      description: Controls which parts of your profile are visible to other users.
      security:
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PrivacySettings"
      responses:
        "200":
          description: Privacy settings updated
          content:
            application/vnd.api+json:
              schema:
                $ref: "#/components/schemas/PrivacyDocument"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/users/me/mood:
    post:
      tags: [Users]
      summary: Submit a mood entry
      description: Records the user's current emotional state. Used for adaptive quest recommendations.
      security:
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [mood]
              properties:
                mood:
                  type: string
                  example: "anxious"
                  description: "Mood keyword (e.g. anxious, lonely, burnout, happy)"
                note:
                  type: string
                  example: "Feeling overwhelmed at work"
      responses:
        "201":
          description: Mood entry created
          content:
            application/vnd.api+json:
              schema:
                $ref: "#/components/schemas/MoodEntryDocument"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/users/me/moods:
    get:
      tags: [Users]
      summary: Get my mood history
      description: Returns all mood entries for the authenticated user, newest first.
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/PageOffset"
        - $ref: "#/components/parameters/PageLimit"
      responses:
        "200":
          description: Mood entry collection
          content:
            application/vnd.api+json:
              schema:
                $ref: "#/components/schemas/MoodEntryCollectionDocument"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/users/me/portfolio:
    get:
      tags: [Portfolio]
      summary: Get my emotional portfolio
      description: |
        Aggregated view of the authenticated user's profile, badges, skill stats,
        quest completion summary, tier, and recent moods. This is the main
        "Emotional Portfolio" endpoint.
      security:
        - BearerAuth: []
      responses:
        "200":
          description: Full emotional portfolio
          content:
            application/vnd.api+json:
              schema:
                $ref: "#/components/schemas/PortfolioDocument"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/users/{userID}/portfolio:
    get:
      tags: [Portfolio]
      summary: Get a user's emotional portfolio
      description: |
        Public view of another user's portfolio. Respects privacy settings —
        returns 403 if the user has hidden their badges and stats.
      parameters:
        - name: userID
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: User's emotional portfolio
          content:
            application/vnd.api+json:
              schema:
                $ref: "#/components/schemas/PortfolioDocument"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  # ── Badges (public) ────────────────────────────────────────────────────────

  /api/v1/badges/:
    get:
      tags: [Badges]
      summary: List all badges
      parameters:
        - $ref: "#/components/parameters/PageOffset"
        - $ref: "#/components/parameters/PageLimit"
      responses:
        "200":
          description: Badge collection
          content:
            application/vnd.api+json:
              schema:
                $ref: "#/components/schemas/BadgeCollectionDocument"

  /api/v1/badges/{id}:
    get:
      tags: [Badges]
      summary: Get badge by ID
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Badge
          content:
            application/vnd.api+json:
              schema:
                $ref: "#/components/schemas/BadgeDocument"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/badges/users/{userID}/badges:
    get:
      tags: [Badges]
      summary: Get badges for a specific user
      description: Respects the user's privacy settings — returns 403 if badges are hidden.
      parameters:
        - name: userID
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: User badge collection with embedded badge details
          content:
            application/vnd.api+json:
              schema:
                $ref: "#/components/schemas/UserBadgeCollectionDocument"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/badges/users/{userID}/skill-stats:
    get:
      tags: [Badges]
      summary: Get detailed skill stats for a specific user
      description: |
        Returns per-category skill breakdown with individual badge contributions.
        Respects the user's privacy settings — returns 403 if stats are hidden.
      parameters:
        - name: userID
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Detailed skill stats by category
          content:
            application/vnd.api+json:
              schema:
                $ref: "#/components/schemas/SkillStatsCollectionDocument"
        "403":
          $ref: "#/components/responses/Forbidden"

  # ── Badges (authenticated) ─────────────────────────────────────────────────

  /api/v1/badges/{id}/award:
    post:
      tags: [Badges]
      summary: Award a badge to yourself
      security:
        - BearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                level:
                  type: integer
                  format: int32
                  minimum: 1
                  default: 1
                  example: 1
      responses:
        "200":
          description: Badge awarded or already at level
          content:
            application/vnd.api+json:
              schema:
                $ref: "#/components/schemas/MessageDocument"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/badges/me/badges:
    get:
      tags: [Badges]
      summary: Get my badges
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/PageOffset"
        - $ref: "#/components/parameters/PageLimit"
      responses:
        "200":
          description: Current user's badge collection
          content:
            application/vnd.api+json:
              schema:
                $ref: "#/components/schemas/UserBadgeCollectionDocument"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/badges/me/skill-stats:
    get:
      tags: [Badges]
      summary: Get my detailed skill stats
      security:
        - BearerAuth: []
      responses:
        "200":
          description: Current user's detailed skill stats by category
          content:
            application/vnd.api+json:
              schema:
                $ref: "#/components/schemas/SkillStatsCollectionDocument"
        "401":
          $ref: "#/components/responses/Unauthorized"

  # ── Quests (public) ────────────────────────────────────────────────────────

  /api/v1/quests/:
    get:
      tags: [Quests]
      summary: List all active quests
      parameters:
        - $ref: "#/components/parameters/PageOffset"
        - $ref: "#/components/parameters/PageLimit"
      responses:
        "200":
          description: Quest collection
          content:
            application/vnd.api+json:
              schema:
                $ref: "#/components/schemas/QuestCollectionDocument"

  /api/v1/quests/{id}:
    get:
      tags: [Quests]
      summary: Get quest by ID
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Quest
          content:
            application/vnd.api+json:
              schema:
                $ref: "#/components/schemas/QuestDocument"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/quests/mood/{mood}:
    get:
      tags: [Quests]
      summary: Adaptive quests by mood tag
      description: |
        Returns active quests whose `mood_tags` array contains the given mood.
        This powers the **Adaptive Quest** system — the frontend passes the user's
        current emotional state (e.g. `lonely`, `anxious`, `burnout`) and receives
        a tailored quest list.
      parameters:
        - name: mood
          in: path
          required: true
          schema:
            type: string
          example: lonely
      responses:
        "200":
          description: Quests matching the mood
          content:
            application/vnd.api+json:
              schema:
                $ref: "#/components/schemas/QuestCollectionDocument"

  # ── Quests (authenticated) ─────────────────────────────────────────────────

  /api/v1/quests/{id}/start:
    post:
      tags: [Quests]
      summary: Start a quest
      description: |
        Assigns the quest to the authenticated user with `active` status.
        A user cannot start the same quest if it is already active.
      security:
        - BearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
          description: Quest ID (from the quest library)
      responses:
        "201":
          description: UserQuest created
          content:
            application/vnd.api+json:
              schema:
                $ref: "#/components/schemas/UserQuestDocument"
              example:
                data:
                  type: user-quests
                  id: "abc123"
                  attributes:
                    status: active
                    started_at: "2024-01-01T10:00:00Z"
                    completed_at: null
                    exp_earned: 0
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Quest already active for this user
          content:
            application/vnd.api+json:
              schema:
                $ref: "#/components/schemas/ErrorDocument"

  /api/v1/quests/{userQuestID}/complete:
    post:
      tags: [Quests]
      summary: Complete a quest
      description: |
        Marks the UserQuest as `completed` and triggers the **Reward Loop**:
        1. Awards `exp_reward` EXP to the user
        2. Recalculates user level (100 EXP = 1 level)
        3. If the quest has a linked `badge_id`, awards or upgrades that badge

        The `id` here is the **UserQuest ID** (returned by the start endpoint),
        not the Quest library ID.
      security:
        - BearerAuth: []
      parameters:
        - name: userQuestID
          in: path
          required: true
          schema:
            type: string
          description: UserQuest ID (from start response)
      responses:
        "200":
          description: Quest completed, EXP awarded
          content:
            application/vnd.api+json:
              schema:
                $ref: "#/components/schemas/UserQuestDocument"
              example:
                data:
                  type: user-quests
                  id: "abc123"
                  attributes:
                    status: completed
                    started_at: "2024-01-01T10:00:00Z"
                    completed_at: "2024-01-01T10:30:00Z"
                    exp_earned: 50
        "400":
          description: Quest is not in active status
          content:
            application/vnd.api+json:
              schema:
                $ref: "#/components/schemas/ErrorDocument"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/quests/{userQuestID}/abandon:
    post:
      tags: [Quests]
      summary: Abandon a quest
      description: Marks the UserQuest as `abandoned`. No EXP is awarded.
      security:
        - BearerAuth: []
      parameters:
        - name: userQuestID
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Quest abandoned
          content:
            application/vnd.api+json:
              schema:
                $ref: "#/components/schemas/UserQuestDocument"
        "400":
          description: Quest is not in active status
          content:
            application/vnd.api+json:
              schema:
                $ref: "#/components/schemas/ErrorDocument"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/quests/me/quests:
    get:
      tags: [Quests]
      summary: Get my quests
      description: |
        Returns all UserQuests for the current user, enriched with full quest details
        in the `relationships.quest` key. Optionally filter by `status`.
      security:
        - BearerAuth: []
      parameters:
        - name: status
          in: query
          required: false
          schema:
            type: string
            enum: [active, completed, abandoned]
        - $ref: "#/components/parameters/PageOffset"
        - $ref: "#/components/parameters/PageLimit"
      responses:
        "200":
          description: User quest collection with embedded quest details
          content:
            application/vnd.api+json:
              schema:
                $ref: "#/components/schemas/UserQuestDetailCollectionDocument"
              example:
                data:
                  - type: user-quests
                    id: "abc123"
                    attributes:
                      status: active
                      started_at: "2024-01-01T10:00:00Z"
                      completed_at: null
                      exp_earned: 0
                    relationships:
                      quest:
                        data:
                          type: quests
                          id: "def456"
                          attributes:
                            name: "Solitude Strength"
                            category: "self-care"
                            exp_reward: 50
                            mood_tags: [lonely]
                            difficulty: easy
                            estimated_duration: 30
        "401":
          $ref: "#/components/responses/Unauthorized"

  # ── Admin: Badges ──────────────────────────────────────────────────────────

  /api/v1/admin/badges/:
    get:
      tags: ["Admin: Badges"]
      summary: List all badges (incl. all categories)
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/PageOffset"
        - $ref: "#/components/parameters/PageLimit"
      responses:
        "200":
          description: Badge collection
          content:
            application/vnd.api+json:
              schema:
                $ref: "#/components/schemas/BadgeCollectionDocument"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

    post:
      tags: ["Admin: Badges"]
      summary: Create a badge
      security:
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name]
              properties:
                name:
                  type: string
                  example: "Burnout Survivor"
                description:
                  type: string
                category:
                  type: string
                  example: "resilience"
                max_level:
                  type: integer
                  format: int32
                  example: 5
                criteria:
                  type: string
                icon:
                  type: string
      responses:
        "201":
          description: Badge created
          content:
            application/vnd.api+json:
              schema:
                $ref: "#/components/schemas/BadgeDocument"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  /api/v1/admin/badges/{id}:
    get:
      tags: ["Admin: Badges"]
      summary: Get badge by ID
      security:
        - BearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Badge
          content:
            application/vnd.api+json:
              schema:
                $ref: "#/components/schemas/BadgeDocument"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

    put:
      tags: ["Admin: Badges"]
      summary: Update a badge
      security:
        - BearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                description:
                  type: string
                category:
                  type: string
                max_level:
                  type: integer
                  format: int32
                criteria:
                  type: string
                icon:
                  type: string
      responses:
        "200":
          description: Badge updated
          content:
            application/vnd.api+json:
              schema:
                $ref: "#/components/schemas/MessageDocument"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

    delete:
      tags: ["Admin: Badges"]
      summary: Delete a badge
      security:
        - BearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Badge deleted
          content:
            application/vnd.api+json:
              schema:
                $ref: "#/components/schemas/MessageDocument"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/admin/badges/{badgeId}/award:
    post:
      tags: ["Admin: Badges"]
      summary: Award a badge to any user
      security:
        - BearerAuth: []
      parameters:
        - name: badgeId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [user_id]
              properties:
                user_id:
                  type: string
                  example: "665f1a2b3c4d5e6f7a8b9c0d"
                level:
                  type: integer
                  format: int32
                  minimum: 1
                  default: 1
      responses:
        "200":
          description: Badge awarded or already at level
          content:
            application/vnd.api+json:
              schema:
                $ref: "#/components/schemas/MessageDocument"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  # ── Admin: Quests ──────────────────────────────────────────────────────────

  /api/v1/admin/quests/:
    get:
      tags: ["Admin: Quests"]
      summary: List all quests (including inactive)
      security:
        - BearerAuth: []
      parameters:
        - $ref: "#/components/parameters/PageOffset"
        - $ref: "#/components/parameters/PageLimit"
      responses:
        "200":
          description: Quest collection
          content:
            application/vnd.api+json:
              schema:
                $ref: "#/components/schemas/QuestCollectionDocument"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

    post:
      tags: ["Admin: Quests"]
      summary: Create a quest
      security:
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name, exp_reward]
              properties:
                name:
                  type: string
                  example: "Solitude Strength"
                description:
                  type: string
                category:
                  type: string
                  example: "self-care"
                exp_reward:
                  type: integer
                  format: int64
                  minimum: 1
                  example: 50
                mood_tags:
                  type: array
                  items:
                    type: string
                  example: [lonely, anxious]
                difficulty:
                  type: string
                  enum: [easy, medium, hard]
                estimated_duration:
                  type: integer
                  format: int32
                  description: Duration in minutes
                badge_id:
                  type: string
                  description: Optional badge to award on completion
                badge_level:
                  type: integer
                  format: int32
                  description: Badge level to award on completion
      responses:
        "201":
          description: Quest created
          content:
            application/vnd.api+json:
              schema:
                $ref: "#/components/schemas/QuestDocument"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  /api/v1/admin/quests/{id}:
    get:
      tags: ["Admin: Quests"]
      summary: Get quest by ID
      security:
        - BearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Quest
          content:
            application/vnd.api+json:
              schema:
                $ref: "#/components/schemas/QuestDocument"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

    put:
      tags: ["Admin: Quests"]
      summary: Update a quest
      security:
        - BearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                description:
                  type: string
                category:
                  type: string
                exp_reward:
                  type: integer
                  format: int64
                mood_tags:
                  type: array
                  items:
                    type: string
                difficulty:
                  type: string
                  enum: [easy, medium, hard]
                estimated_duration:
                  type: integer
                  format: int32
                badge_id:
                  type: string
                badge_level:
                  type: integer
                  format: int32
                is_active:
                  type: boolean
      responses:
        "200":
          description: Quest updated
          content:
            application/vnd.api+json:
              schema:
                $ref: "#/components/schemas/MessageDocument"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

    delete:
      tags: ["Admin: Quests"]
      summary: Delete a quest
      security:
        - BearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Quest deleted
          content:
            application/vnd.api+json:
              schema:
                $ref: "#/components/schemas/MessageDocument"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
